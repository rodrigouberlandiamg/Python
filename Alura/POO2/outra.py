from abc import ABC # Classes abstratas básicas
from collections.abc import MutableSequence
from numbers import Complex
class Playlist(Complex):
    def __getitem__(self, item):
        super().__getitem__()

filmes = Playlist()